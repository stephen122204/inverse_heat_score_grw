# Python 3.11 (developed and frozen on CPython 3.11.4)
# Pinned exact versions so results reproduce bit-for-bit.
# NOTE: numpy>=2.0 removed np.trapz (renamed np.trapezoid); the code uses a
# compatibility shim, so both 1.26.x and 2.x work, but 2.4.4 is the frozen pin.
numpy==2.4.4
scipy==1.17.1
pandas==2.2.3
matplotlib==3.10.8
pyyaml==6.0.3
# Test dependency (Amendment 1 housekeeping): the contract suite runs under pytest.
pytest==7.4.0
